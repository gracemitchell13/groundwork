# Firebase
.firebase/
firebase-debug.log
.firebaserc

# Environment / secrets — never commit real credentials
# Keep firebase-config.js in git but with placeholder values only
# Use environment injection or a local override for real values

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
js/anthropic-config.js
